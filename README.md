# WORKL 採用支援LP（確認用プレビュー）

クライアント確認用にGitHub Pagesで公開しているLPです。

## 導線

- `index.html` … LP本体（フロントページ）
- `form.html` … 資料請求フォーム
- `thanks.html` … サンクスページ（送信完了）

## 注意

- 検索エンジンには登録されない設定です（全ページ `noindex` ＋ `robots.txt` でDisallow）。
- フォームの送信先は未設定です。送信するとデータは送信されず、サンクスページへ遷移するのみです。
